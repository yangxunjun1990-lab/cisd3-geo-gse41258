# AGENTS.md

本文件定义本仓库中所有自动化/分析脚本的协作规范。

## 核心原则
1. **不编造结果**：
   - 不得凭空生成统计结果、图表结论或显著性结论；
   - 任何结论必须来自可追溯的数据处理与统计流程。

2. **原始数据只读**：
   - `data_raw/` 下的原始文件视为只读；
   - 禁止脚本直接覆盖、修改原始数据。

3. **统一使用相对路径**：
   - 所有脚本中路径必须使用相对路径；
   - 禁止硬编码绝对路径（例如 `/home/...`、`C:\...`）。

## 输出规范
1. **图形输出目录**：
   - 所有生成图像统一写入 `results/figures/`。

2. **表格输出目录**：
   - 所有生成表格统一写入 `results/tables/`。

## 脚本文档规范
每个脚本（尤其是 `scripts/` 下的 R 脚本）必须在文件开头写清楚：
- **输入**：脚本读取哪些文件；
- **输出**：脚本产出哪些文件；
- **用途**：脚本用于完成哪一步分析。

建议额外补充：运行方式、依赖包、参数说明。
